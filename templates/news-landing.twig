{% extends 'base.twig' %} {% block content %}

<div class="container news-section">
	<div class="inner">
		<h4>LATEST NEWS</h4>
		<div class="row">
			{# <div class="col-xs-12">
				{% for post in featured %}
				<a href="{{post.link}}">
					<div class="card">
						<div class="image-container">
							<img class="card-img-top" src="{{post.get_field('featured_banner')}}" alt="Card image cap">
							<div class="image-overlay"></div>
						</div>
						<div class="card-block">
							<p class="card-text">{{post.post_excerpt}}</p>
							<p class="card-date">{{post.date}}</p>
						</div>
					</div>
				</a>
				{% endfor %}
			</div> #}
			<a href="http://idwent.dev/download/101/" download>test</a>
			{% for post in posts %}
				<div class="col-xs-12 col-sm-6 col-md-4">
			 	{% include 'news-loop.twig' %} 
			 	</div>
			 {% endfor %}

		</div>
	</div>
</div>

{% endblock %}