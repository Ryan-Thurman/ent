{% extends 'base.twig' %}

{% block content %}
    <div class="container-fluid">
    {{title}}
        {#{% for post in posts %}
            <h4>{{post.title}}</h4>
        {% endfor %}#}
    </div>
{% endblock %}